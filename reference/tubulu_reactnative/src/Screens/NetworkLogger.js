import React from 'react';
import NetworkLogger from 'react-native-network-logger';

function NetworkLoggerScreen(props) {
  return <NetworkLogger theme={'dark'} />;
}

export default NetworkLoggerScreen;
